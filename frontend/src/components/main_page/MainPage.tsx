import styles from './styles.module.scss'

const MainPage = () => {
	return (
		<div className={`${styles.mainPageText} container`}>
			<h1>ГЛАВНАЯ СТРАНИЦА ПРИ ВХОДЕ НА САЙТ</h1>
		</div>
	)
}

export default MainPage
